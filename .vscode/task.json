{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Project",   // 这里的名字是你自己定义的
            "type": "cmake",            //这是关键，指定使用CMake插件
            "command": "build",         // 执行 build 命令
            "problemMatcher": "$gcc",   // 捕获编译错误
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "CMake 构建任务"
        }
    ]
}